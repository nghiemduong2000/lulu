import { Playpen_Sans, Roboto_Condensed, Spectral } from "next/font/google";

export const playpen_sans = Playpen_Sans({
    subsets: ['latin'],
    weight: ['400', '500', '700'],
});

export const spectral = Spectral({
    subsets: ['latin'],
    weight: ['400', '500', '700'],
});

export const roboto_condensed = Roboto_Condensed({
    subsets: ['latin'],
    weight: ['400', '500', '700'],
});